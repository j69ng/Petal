node_modules/
.next/
out/
# The database and its WAL sidecars — your books, not code.
data/*.db
data/*.db-*
.env.local
*.tsbuildinfo
next-env.d.ts

# Your signature and the signed documents. Personal, and a signature image in a
# repository is a signature anyone with the repository can reuse.
docs/signature.png
docs/*.pdf
